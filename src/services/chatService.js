// Placeholder for API call functions
export const fetchChatResponse = async (message, mood) => {
    // Replace this with API calls to your AI backend
    return `Response to "${message}" in ${mood} mood`;
  };
  